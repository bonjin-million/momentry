import 'package:flutter/material.dart';

class BookListBody extends StatelessWidget {
  const BookListBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('등록된 책이 없어요'),
    );
  }
}
