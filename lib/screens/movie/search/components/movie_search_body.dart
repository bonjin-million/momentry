import 'package:flutter/material.dart';

class MovieSearchBody extends StatelessWidget {
  const MovieSearchBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
